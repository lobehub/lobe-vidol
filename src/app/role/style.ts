import { createStyles } from 'antd-style';

export const useStyles = createStyles(({ css }) => ({
  preview: css`
    overflow: auto;
    width: 80rem;
    margin: 0 auto;
  `,
  edit: css`
    padding: 0 24px;
  `,
  model: css``,
}));
